{
  "name": "effective-assistant-runner",
  "version": "0.0.1",
  "description": "Long-running agent container that consumes events from the bus-state postgres.",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "format": "biome format --write --config-path .. src/",
    "format:check": "biome format --config-path .. src/",
    "lint": "biome lint --config-path .. src/",
    "check": "biome check --config-path .. src/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@ai-sdk/openai-compatible": "^2.0.45",
    "ai": "^6.0.174",
    "effective-assistant-shared": "file:../shared",
    "nodemon": "^3.1.7",
    "pino": "^9.14.0",
    "tsx": "^4.20.4",
    "typescript": "^6.0.3",
    "yaml": "^2.8.4"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.12",
    "@types/node": "^25.6.0"
  }
}
